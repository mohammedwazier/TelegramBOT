// const request = require('request');
const Api = require('../Helper/ApiHelper')
const Config = require('../Config/Config')

const url = Config.UrlRoute;

// console.log(url)

const sendMessage = (message) => {
	// console.log('asdads')
}

module.exports = {
	sendMessage
}